# Examples

Store small, runnable examples here.

Examples should:

- Use fake, public, or synthetic data.
- Run without hidden credentials.
- Demonstrate the intended public interface.
- Avoid duplicating production logic.

Move reusable code into `src/` and import it from examples.
