# Processed Data

Use this folder for deterministic processed datasets created by repository workflows.

Prefer storing only manifests and reproduction instructions in Git. Store large artifacts in approved object storage, release artifacts, or a data registry.
