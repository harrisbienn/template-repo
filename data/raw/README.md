# Raw Data

Place immutable source files here for local work. Treat this folder as read-only once files are staged.

Real raw data is ignored by Git. Commit only small documentation files or synthetic fixtures that are approved for source control.
