# Integration Tests

Use this folder for tests that touch files, databases, APIs, cloud services, or external tools.

Mark tests that require credentials, network access, large data, or long runtimes so CI can select the right subset.
