# End-To-End Tests

Use this folder for full workflow tests that exercise the system from a user-facing entry point to expected output.

Keep these tests small and deterministic. Use smoke tests for expensive workflows.
