# Tools

Store developer tooling here.

Examples:

- Code generation helpers.
- Repository maintenance utilities.
- Static analysis wrappers.
- Release note helpers.

Tools are not product runtime code. If users or deployed systems depend on it, move it into `src/`.
